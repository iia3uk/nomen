# Copy to `.env` and fill in keys you actually use.
# All keys are optional; missing ones only reduce online check depth.

GITHUB_TOKEN=
BRAVE_SEARCH_API_KEY=
SERPAPI_KEY=
BING_SEARCH_API_KEY=
OPENCORPORATES_API_TOKEN=
WHOIS_API_KEY=
